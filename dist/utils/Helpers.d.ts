/**
 * Helper function used to determine if an error thrown by the server requires interaction to resolve
 * @param error
 */
export declare function IsAnInteractionRequiredAuthError(error: any): boolean;
