describe('Fetch manager tests', () => {
    it('should fetch data from an unprotected endpoint', () => {
        expect(1).toBe(1);
    });

    it('should fetch data from a protected endpoint', () => {
        expect(1).toBe(1);
    });

    it('should handle pagination', () => {
        expect(1).toBe(1);
    });
})