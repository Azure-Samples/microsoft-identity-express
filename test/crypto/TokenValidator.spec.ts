describe('Token validator tests', () => {
    it('should get signing keys', () => {
        expect(1).toBe(1);
    });

    it('should instantiate as type', () => {
        expect(1).toBe(1);
    });
    
    it('should decode an access token', () => {
        expect(1).toBe(1);
    });

    it('should decode an id token', () => {
        expect(1).toBe(1);
    });

    it('should validate access token claims given settings', () => {
        expect(1).toBe(1);
    });

    it('should validate id token claims given settings', () => {
        expect(1).toBe(1);
    });
})